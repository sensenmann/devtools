id = "node_dependency_update_force"
name = "Node dependency update (force)"
description = "Run npm audit fix --force for all package.json locations inside the selected project."
project_types = ["node"]
entry = "nodeDependencyUpdate"

[default_args]
force = true
registry = "https://registry.npmjs.org"
