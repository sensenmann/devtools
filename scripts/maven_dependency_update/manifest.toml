id = "maven_dependency_update"
name = "☕ Maven dependency update"
description = "Run mvn versions:use-latest-releases for all pom.xml locations inside the selected project."
project_types = ["maven"]
entry = "mavenDependencyUpdate"
group = "Dependency Update"
variant_key = "allow_major_updates"
variant_values = ["minor", "major"]
variant_default = "minor"

[variant_arg_values]
minor = false
major = true
