id = "node_dependency_update"
name = "📦 Node dependency update"
description = "Run npm audit fix for all package.json locations inside the selected project."
project_types = ["node"]
entry = "nodeDependencyUpdate"
group = "Dependency Update"
variant_key = "force"
variant_values = ["normal", "force"]
variant_default = "normal"

[default_args]
registry = "https://registry.npmjs.org"

[variant_arg_values]
normal = false
force = true
