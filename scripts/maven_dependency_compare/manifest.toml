id = "maven_dependency_compare"
name = "☕  Maven dependency compare"
description = "Compare parent POMs and dependency versions across the selected Maven projects in an interactive browser report."
project_types = ["maven"]
scope = "selection"
entry = "mavenDependencyCompare"
group = "Dependency Alignment"
variant_key = "mode"
variant_values = ["fast", "deep"]
variant_default = "deep"

[variant_arg_values]
fast = "fast"
deep = "deep"
